<%= @question.question %>
<%= simple_form_for :answer do |f| %>
  <%= f.select :answer, Option.all.select{ |o| o.question_id === @question.id }.map{ |r| r.answer }  %>
  <%= f.button :submit %>
<% end %>

<% @question.answers.each do |a| %>
  <%= a.answer %>
<% end %>
